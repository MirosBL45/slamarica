import styles from './forsave.module.scss';

export default function ForSave() {
  return (
    <div className={styles.blog}>
      <h1 className={styles.title}>ForSave</h1>
      <p className={styles.content}>This is the ForSave component.</p>
    </div>
  );
}
